import React from "react";
import { Header } from "../components/header";
interface homeProps {}

const Home: React.FC<homeProps> = ({}) => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
